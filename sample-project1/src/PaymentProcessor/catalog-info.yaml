apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: payment-processor
  description: The Payment Processor is a microservice that processes payments.
  annotations:
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner: eshop-admin
  lifecycle: experimental
  system: eshop-ordering
  domain: eshop
  dependsOn:
    - resource:eventbus
