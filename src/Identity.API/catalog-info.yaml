apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: identity-api
  description: The Identity API is a microservice that handles user authentication and identity.
  annotations:
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner: default/rocket-team
  lifecycle: experimental
  system: eshop-web
  domain: eshop
  providesApis:
    - identity-api
  dependsOn:
    - resource:eventbus
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: identity-api
  description: The Identity API
spec:
  type: openapi
  lifecycle: experimental
  owner: default/rocket-team
  system: eshop-web
  domain: eshop
  definition:
    $text: https://raw.githubusercontent.com/Test-orgn154/backstage-demo/main/src/Identity.API/swagger.json